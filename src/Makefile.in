# Makefile.in generated by automake 1.8.5 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004  Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.
CXX = @CXX@
DEFS = @DEFS@
DEFAULT_INCLUDES = -I. 
CPPFLAGS = @CPPFLAGS@
CXXFLAGS = @CXXFLAGS@
CXXCOMPILE = $(CXX) $(DEFS) $(DEFAULT_INCLUDES) $(INCLUDES) \
	$(AM_CPPFLAGS) $(CPPFLAGS) $(AM_CXXFLAGS) $(CXXFLAGS) 
SHELL = @SHELL@
LIB_CXXFLAGS=@LIB_CXXFLAGS@
LIB_LDFLAGS=@LIB_LDFLAGS@

###################################################################################
#write your own rules here                                                        #
###################################################################################

PROG_RDB=rdb
PROG_ROUTE=route
PROG_STORE=store

OBJ_PUB=iniconfig.o minilog.o strutil.o \
		rdbconf.o rdbutil.o rdbsignal.o

OBJ_RDB=$(OBJ_PUB) rdb.o
OBJ_ROUTE=$(OBJ_PUB) route.o
OBJ_STORE=$(OBJ_PUB) store.o

SRC_PUB=$(OBJ_PUBL:.o=.cpp)
SRC_RDB=$(OBJ_RDB:.o=.cpp)
SRC_ROUTE=$(OBJ_ROUTE:.o=.cpp)
SRC_STORE=$(OBJ_STORE:.o=.cpp)


EXT_CPPFLAGS=
EXT_LDFLAGS=

all:$(PROG_RDB) $(PROG_ROUTE) $(PROG_STORE)

dep_pub:$(SRC_PUB)
	makedepend $(DEFAULT_INCLUDES) $(INCLUDES) $(EXT_CPPFLAGS) $(SRC_PUB) 2>/dev/null

dep_rdb:dep_pub $(SRC_RDB)
	makedepend $(DEFAULT_INCLUDES) $(INCLUDES) $(EXT_CPPFLAGS) $(SRC_RDB) 2>/dev/null

$(PROG_RDB): dep_rdb $(OBJ_RDB)
	$(CXXCOMPILE) $(EXT_CPPFALGS) $(EXT_LDFALGS) -o $@ $(OBJ_RDB)

dep_route:dep_pub $(SRC_ROUTE)
	makedepend $(DEFAULT_INCLUDES) $(INCLUDES) $(EXT_CPPFLAGS) $(SRC_ROUTE) 2>/dev/null

$(PROG_ROUTE): dep_route $(OBJ_ROUTE)
	$(CXXCOMPILE) $(EXT_CPPFALGS) $(EXT_LDFALGS) -o $@ $(OBJ_ROUTE)

dep_store:dep_pub $(SRC_LRDB)
	makedepend $(DEFAULT_INCLUDES) $(INCLUDES) $(EXT_CPPFLAGS) $(SRC_LRDB) 2>/dev/null

$(PROG_STORE): dep_store $(OBJ_STORE)
	$(CXXCOMPILE) $(EXT_CPPFALGS) $(EXT_LDFALGS) -o $@ $(OBJ_STORE)

install:$(PROG_RDB) $(PROG_NETWAY) $(PROG_ROUTE) $(PROG_STORE)
	rm -f @prefix@/bin/$(PROG_RDB)
	rm -f @prefix@/bin/$(PROG_ROUTE)
	rm -f @prefix@/bin/$(PROG_STORE)
	cp $(PROG_RDB) @prefix@/bin/$(PROG_RDB)
	cp $(PROG_ROUTE) @prefix@/bin/$(PROG_ROUTE)
	cp $(PROG_STORE) @prefix@/bin/$(PROG_STORE)

clean:
	rm -f *.o
	rm -f $(PROG_RDB)
	rm -f $(PROG_ROUTE)
	rm -f $(PROG_STORE)

include @prefix@/rule.mk

